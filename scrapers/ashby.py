def fetch_ashby_jobs(company: str, careers_url: str):
    """
    Placeholder for Ashby scraper.

    Ashby has an authenticated public API that requires organization-level API keys.
    Without an API key for each company, we can't reliably fetch their postings.

    For now, we just log and return an empty list so the rest of the system works.
    """
    print(f"[INFO] Ashby scraper for {company} is not implemented yet. careers_url={careers_url}")
    return []
